import React from "react";

import CartOfProducts from "../components/utils/CartOfProducts";

const Cart = (): JSX.Element => {
    return (
        <CartOfProducts className='mx-auto container flex-1 relative min-h-[300px]' />
    );
};

export default Cart;
