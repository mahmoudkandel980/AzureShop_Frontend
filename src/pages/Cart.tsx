import React from "react";

import CartOfProducts from "../components/utils/CartOfProducts";

const Cart = (): JSX.Element => {
    return <CartOfProducts className='mx-auto container flex-1 relative' />;
};

export default Cart;
